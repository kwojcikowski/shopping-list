import React from 'react';

const SectionWidget = ({section, cart, products}) => {

    return (
        <>
            <h4>{section.name}</h4>
            <table>
                <tbody>
                <tr>
                    <td></td>
                </tr>
                </tbody>
            </table>
        </>
    )
}

export default SectionWidget;
